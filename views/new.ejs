<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Listing</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #74ebd5, #9face6);
            animation: gradientChange 5s infinite alternate ease-in-out;
        }

        /* Form Container */
        .form-container {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            width: 90%;
            max-width: 400px;
            text-align: center;
            box-sizing: border-box;
            animation: bounceIn 1s ease-in-out;
        }

        h3 {
            margin-bottom: 15px;
            color: #333;
        }

        /* Input Fields */
        input {
            width: 100%;
            padding: 10px;
            margin: 8px 0;
            border: 2px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
            transition: all 0.3s ease-in-out;
            box-sizing: border-box;
        }

        /* Hover & Focus Effects */
        input:hover, input:focus {
            border-color: #74ebd5;
            box-shadow: 0 0 10px rgba(116, 235, 213, 0.5);
            transform: scale(1.02);
            animation: shake 0.3s ease-in-out;
        }

        /* Submit Button */
        button {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, #74ebd5, #9face6);
            border: none;
            color: white;
            font-size: 18px;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            box-sizing: border-box;
        }

        /* Shudder Effect on Hover */
        button:hover {
            background: linear-gradient(135deg, #9face6, #74ebd5);
            animation: shudder 0.2s infinite;
        }

        /* Keyframe Animations */
        @keyframes bounceIn {
            0% { transform: scale(0.5); opacity: 0; }
            70% { transform: scale(1.05); }
            100% { transform: scale(1); opacity: 1; }
        }

        @keyframes shake {
            0% { transform: translateX(0); }
            25% { transform: translateX(-5px); }
            50% { transform: translateX(5px); }
            75% { transform: translateX(-5px); }
            100% { transform: translateX(0); }
        }

        @keyframes shudder {
            0% { transform: translateX(0); }
            25% { transform: translateX(-1px); }
            50% { transform: translateX(1px); }
            75% { transform: translateX(-1px); }
            100% { transform: translateX(0); }
        }

        @keyframes gradientChange {
            0% { background: linear-gradient(135deg, #74ebd5, #9face6); }
            100% { background: linear-gradient(135deg, #ff758c, #ff7eb3); }
        }

        /* Responsive Design */
        @media (max-width: 600px) {
            .form-container {
                width: 95%;
            }
        }

    </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="form-container">
        <h3>Create a New Listing</h3>
        <form enctype="multipart/form-data" method="post" action="/listings" class="needs-validation row g-3 ">
            <input type="text" name="Listing[title]" placeholder="Enter title" required >
            <input type="text" name="Listing[description]" placeholder="Enter description" required>
            <!-- <input type="url" name="Listing[image]" placeholder="Enter image URL" required> -->
            <input type="file" name="Listing[image]" placeholder="Enter image URL" required>
            <input type="number" name="Listing[price]" placeholder="Enter price" min="0" required>
            <input type="text" name="Listing[location]" placeholder="Enter location" required>
            <input type="text" name="Listing[country]" placeholder="Enter country" required>
            <button type="submit">Submit</button>
        </form>
    </div>
<script src="./js/validation.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
