<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Your Listing</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: "Segoe UI", sans-serif;
      background-color: #f5f7fa;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .form-container {
      background-color: #ffffff;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
      width: 100%;
      max-width: 450px;
      animation: fadeIn 0.5s ease-in-out;
    }

    h3 {
      text-align: center;
      font-size: 22px;
      margin-bottom: 25px;
      color: #333;
    }

    .form-group {
      position: relative;
      margin-bottom: 20px;
    }

    input, textarea {
      width: 100%;
      padding: 14px 12px;
      font-size: 15px;
      border: 1.5px solid #ccc;
      border-radius: 6px;
      background-color: #fafafa;
      transition: border-color 0.3s, background-color 0.3s;
      outline: none;
    }

    input:focus, textarea:focus {
      border-color: #007bff;
      background-color: #fff;
    }

    label {
      position: absolute;
      left: 12px;
      top: 50%;
      transform: translateY(-50%);
      font-size: 14px;
      color: #999;
      pointer-events: none;
      transition: 0.3s ease all;
      background-color: white;
      padding: 0 4px;
    }

    input:focus + label,
    input:not(:placeholder-shown) + label,
    textarea:focus + label,
    textarea:not(:placeholder-shown) + label {
      top: -8px;
      left: 8px;
      font-size: 12px;
      color: #007bff;
    }

    button {
      width: 100%;
      padding: 12px;
      background-color: #007bff;
      color: #fff;
      font-size: 16px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.2s;
    }

    button:hover {
      background-color: #005ec2;
      transform: translateY(-2px);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 500px) {
      .form-container {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="form-container">
    <%- include('./lincludes/flash.ejs') %>
    <h3>Edit Your Listing</h3>
    <form enctype="multipart/form-data" method="post" action="/listings/<%= userData._id %>?_method=PUT">
      <div class="form-group">
        <input type="text" name="Listing[title]" id="title" value="<%= userData.title %>" placeholder=" " required />
        <label for="title">Title</label>
      </div>
      <div class="form-group">
        <textarea name="Listing[description]" id="description" placeholder=" " rows="4" required><%= userData.description %></textarea>
        <label for="description">Description</label>
      </div>
      <div class="form-group">
        <!-- <p><%=userData.image.url  %></p> -->
        <input type="file" name="Listing[image]" id="image"  placeholder=" " />
        <label for="image">New Image</label>
      </div>
      <div class="form-group">
        <input type="number" name="Listing[price]" id="price" value="<%= userData.price %>" placeholder=" " required />
        <label for="price">Price</label>
      </div>
      <div class="form-group">
        <input type="text" name="Listing[location]" id="location" value="<%= userData.location %>" placeholder=" " required />
        <label for="location">Location</label>
      </div>
      <div class="form-group">
        <input type="text" name="Listing[country]" id="country" value="<%= userData.country %>" placeholder=" " required />
        <label for="country">Country</label>
      </div>
      <button type="submit">Save Changes</button>
    </form>
  </div>
</body>
</html>
