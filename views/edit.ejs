<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Listing</title>
    <style>
        /* General Body Styling */
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #ff9a9e, #fad0c4);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* Form Container */
        .form-container {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            animation: fadeIn 1s ease-in-out;
        }

        /* Heading Styling */
        h3 {
            font-size: 24px;
            text-align: center;
            margin-bottom: 20px;
            color: #333;
        }

        /* Form Element Styling */
        .form-group {
            position: relative;
            margin-bottom: 20px;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 12px 15px;
            margin: 0;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 16px;
            background-color: #f9f9f9;
            box-sizing: border-box;
            transition: all 0.3s ease;
        }

        /* Fixes the overlap issue */
        .form-group input:focus,
        .form-group textarea:focus {
            border-color: #5e81ac;
            background-color: #fff;
            box-shadow: 0 0 8px rgba(94, 129, 172, 0.5);
        }

        /* Label Styling */
        .form-group label {
            position: absolute;
            top: 50%;
            left: 15px;
            font-size: 16px;
            color: #aaa;
            transform: translateY(-50%); /* Vertically center the label */
            pointer-events: none; /* Make sure label does not interfere with input */
            transition: all 0.3s ease;
        }

        /* Float the label above input field when focused */
        .form-group input:focus + label,
        .form-group textarea:focus + label {
            top: -10px;
            font-size: 12px;
            color: #5e81ac;
        }

        /* Button Styling */
        button {
            width: 100%;
            padding: 14px;
            background-color: #5e81ac;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 20px;
        }

        button:hover {
            background-color: #4c6c8c;
            transform: translateY(-2px);
        }

        button:active {
            transform: translateY(1px);
            background-color: #3c5c7b;
        }

        /* Animation Effects */
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(-30px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive Design */
        @media (max-width: 600px) {
            .form-container {
                padding: 20px;
            }

            h3 {
                font-size: 20px;
            }

            button {
                font-size: 16px;
            }
        }
    </style>
</head>
<body>

    <div class="form-container">
        <h3>Edit Your Listing</h3>
        <form method="post" action="/listings/<%= userData._id %>?_method=PUT">
            <div class="form-group">
                <input type="text" name="Listing[title]" id="title" value="<%= userData.title %>" placeholder=" ">
                <label for="title">Title</label>
            </div>
            <div class="form-group">
                <textarea name="Listing[description]" id="description" placeholder=" " rows="4"><%= userData.description %></textarea>
                <label for="description">Description</label>
            </div>
            <div class="form-group">
                <input type="text" name="Listing[image]" id="image" value="<%= userData.image %>" placeholder=" ">
                <label for="image">Image URL</label>
            </div>
            <div class="form-group">
                <input type="number" name="Listing[price]" id="price" value="<%= userData.price %>" placeholder=" ">
                <label for="price">Price</label>
            </div>
            <div class="form-group">
                <input type="text" name="Listing[location]" id="location" value="<%= userData.location %>" placeholder=" ">
                <label for="location">Location</label>
            </div>
            <div class="form-group">
                <input type="text" name="Listing[country]" id="country" value="<%= userData.country %>" placeholder=" ">
                <label for="country">Country</label>
            </div>
            <button type="submit">Edit Listing</button>
        </form>
    </div>

</body>
</html>
